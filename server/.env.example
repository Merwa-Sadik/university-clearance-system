PORT=5000

# MySQL — use values from your Render MySQL instance
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=student_clearance
DB_SSL=false

JWT_SECRET=change_this_to_a_long_random_secret_string
JWT_EXPIRES_IN=1d

# Set this to your Vercel frontend URL in production
CLIENT_URL=http://localhost:5173
